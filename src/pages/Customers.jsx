import {useEffect,useState} from 'react';import{Plus}from'lucide-react';import{supabase}from'../lib/supabase'
export default function Customers(){const[rows,setRows]=useState([]);const[form,setForm]=useState({full_name:'',phone:'',email:'',notes:''});const[show,setShow]=useState(false);useEffect(()=>{load()},[]);async function load(){const{data}=await supabase.from('customers').select('*').order('created_at',{ascending:false});setRows(data||[])}async function save(e){e.preventDefault();const{error}=await supabase.from('customers').insert({...form,email:form.email||null,notes:form.notes||null});if(error)return alert(error.message);setForm({full_name:'',phone:'',email:'',notes:''});setShow(false);load()}
return <><section className="panel"><div className="panel-title"><div><span className="eyebrow">CLIENTES</span><h2>Base de clientes</h2></div><button className="primary compact" onClick={()=>setShow(!show)}><Plus size={18}/>Nuevo cliente</button></div>{show&&<form className="inline-form" onSubmit={save}><label>Nombre<input required value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})}/></label><label>Teléfono<input required value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Correo<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label className="wide">Notas<textarea value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></label><button className="primary">Guardar cliente</button></form>}<div className="table-wrap"><table><thead><tr><th>Nombre</th><th>Teléfono</th><th>Correo</th><th>Notas</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.full_name}</td><td>{r.phone}</td><td>{r.email||'—'}</td><td>{r.notes||'—'}</td></tr>)}</tbody></table></div>{!rows.length&&<div className="empty">Todavía no hay clientes registrados.</div>}</section></>}
